import { AnswerDirDirective } from './answer-dir.directive';

describe('AnswerDirDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerDirDirective();
    expect(directive).toBeTruthy();
  });
});
